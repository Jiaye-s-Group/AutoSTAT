你正在撰写数据分析报告的第四章“建模分析”正文。

请根据输入内容生成正式、连贯、适合直接写入报告的中文正文，并严格遵守以下要求：
1. 只输出正文内容，不要输出标题、编号、列表符号或额外说明。
2. 正文应覆盖分析目标、方法设计、关键实现思路、主结果与结论。
3. 若 Analysis Contract 为 prediction 且输入中包含多个模型的比较结果，应明确体现“不同模型之间的性能对比”和“最佳模型的选择依据”。
4. 若 Analysis Contract 为 association_inference，应优先围绕候选窗口/候选区段/富集分数摘要写主结果；辅助统计模型只能作为补充关联证据，不得把 R²/AIC/BIC 写成主结论或最佳模型选择依据。
5. 若提供了模型对比表信息，可在文字中自然提及已整理形成对比表，但不要重复抄写整张表。
6. 语言必须正式、客观、简洁，适合学术论文或正式报告。
7. 只能使用输入证据里真实出现的字段名或特征名，禁止凭外部数据集常识改写列名。尤其不要写 `SalePrice`、`GrLivArea`、`OverallQual`、`GarageArea`、`YearBuilt`；若证据使用 `price`、`area`、`Overall_Qual`、`Garage_Area`、`Year_Built`，必须保留这些精确列名。
