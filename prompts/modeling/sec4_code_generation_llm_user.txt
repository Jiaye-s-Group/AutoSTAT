示例数据：
{{df_head}}

Requirement（请根据以下建模任务指令，对所有列出的模型依次执行训练与评估。若某模型在当前环境不可用，请手动实现对应算法或类，使结果完整可复现）：
{{refined_suggestions}}

允许使用的库：numpy, sklearn.model_selection, sklearn.preprocessing, sklearn.ensemble, torch, torchvision, xgboost, lightgbm。
???????? XGBoost????? `use_label_encoder` ???????????????????????

=== 用户需求与偏好 ===
- 用户需求：{{user_input}}
- 用户的分析偏好设置：{{preference_selected}}
- 用户的建模目的与特殊需求（高优先级，务必满足）：{{add_preference}}
- 用户上传的参考资料（业务背景/算法说明/数据手册，如有相关内容请优先参考）：{{ref_context}}

返回
1.完整能正确运行的 Python 代码（纯代码块），不允许包含任何文字。
2.脚本末尾仅包含一行 `result_dict = {...}` 赋值。
3.不输出任何分析、解释或说明（包括文字、列表或注释段落）
- 运行时已经直接提供全量 `df`，不要写任何 `df = ...`。
- `{{df_head}}` 只是全量数据的预览，只能用于理解列名、类型和示例值，绝不能据此重新构造 `df` 或覆盖 `df`。
- 如果需要筛选、采样、聚合、清洗或特征工程，请使用新变量名，例如 `work_df = df.copy()`、`model_df = ...`、`train_df = ...`。
- 一旦输出中出现 `df = pd.DataFrame(...)`、`df = pd.read_*...`、`df = df.head(...)`、`df = df.sample(...)`、`df = {...}`、`df = [...]`，都视为错误代码。

DF rules:
- The runtime already provides the full dataset as `df`. Never write any `df = ...`.
- `{{df_head}}` is only a preview for understanding columns and sample values. Never rebuild or overwrite `df` from it.
- If you need filtering, sampling, aggregation, cleaning, or feature engineering, use a new variable such as `work_df = df.copy()`, `model_df = ...`, or `train_df = ...`.
- If the output contains `df = pd.DataFrame(...)`, `df = pd.read_*...`, `df = df.head(...)`, `df = df.sample(...)`, `df = {...}`, or `df = [...]`, it is wrong.
