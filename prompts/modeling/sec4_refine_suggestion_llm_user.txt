请阅读以下详细建模建议，并将其保真精炼为对下一个 coding agent 的清晰建模任务指令。精炼结果必须逐项覆盖详细建议中的每一个具体模型、训练步骤、评估指标和输出要求，不能因为简化而遗漏前端已展示的方案。

详细建模建议：
{{model_suggestion}}

数据预览：
{{df_head}}

可用字段：
{{columns}}

当前建模目标：
{{target}}

用户需求与偏好：
用户需求：{{user_input}}
用户选择或补充的建模意图：{{user_prompt}}
用户的分析偏好设置：{{preference_selected}}
用户的建模目的与特殊需求：{{add_preference}}
用户上传的参考资料：{{ref_context}}

输出要求（必须严格遵守）：
1. 只输出纯文本，不使用 Markdown 标记、标题、编号、项目符号、代码块或解释。
2. 每行写一个可执行任务要求。
3. 必须保留详细建议中出现的所有模型名称、超参数、特征处理、训练/验证方式、评估指标、模型比较和输出产物。
4. 如果详细建议列出多个模型，必须要求 coding agent 逐一训练、评估并比较所有模型，不得只保留其中一个。
5. 如果详细建议包含可选方案，必须明确保留为可选候选；不要擅自删除。
6. 不要新增详细建议中没有出现的模型、指标或业务分析角度。
