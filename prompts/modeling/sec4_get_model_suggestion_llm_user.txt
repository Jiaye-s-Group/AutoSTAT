=== 数据信息 ===
数据列名: {{columns}}

数据前5行:
{{df_head}}


=== 建模目标 ===
{{target}}
（请务必满足该目标，并在回答中明确复述建模意图。）

=== 用户需求与偏好 ===
- 用户需求：{{user_input}}
- 用户的分析偏好设置：{{preference_selected}}
- 用户的建模目的与特殊需求（高优先级，务必满足）：{{add_preference}}
- 用户上传的参考资料（业务背景/算法说明/数据手册，如有相关内容请优先参考）：{{ref_context}}

=== 历史训练代码 ===
{{train_code}}

=== 建模建议任务指令 ===
请检测上述“历史训练代码”内容：

请先判断“用户需求”是否为用户自己输入的具体建模指令：
- 如果为空，或只是“请帮我获取建模建议”这类默认请求，请按下方情况A/情况B保持原有默认流程。
- 如果包含具体目标列、模型名称、优化方向、指标、调参、特征工程、训练限制或解释性要求，请优先围绕用户需求输出建议；情况A/情况B只作为组织方式，不得覆盖用户指令。
- 如果用户快速选择了模型或补充了建模意图（见“用户需求与偏好”中的相关内容），必须把这些模型或意图纳入建议。
- 如果用户指定字段不存在，请明确指出，并不要围绕不存在字段设计建模流程。

【情况A：如果历史训练代码不为空】
请在充分理解上述代码的基础上，提出 **1–2 条高质量的模型改进建议**。
可从以下角度思考，但不限于此：
- 模型结构优化（如增加层数、调整激活函数、替换模型类型等）；
- 特征工程改进（如变量选择、特征交互、归一化策略等）；
- 训练流程优化（如正则化、学习率调度、损失函数调整等）；
- 超参数调整（如树深度、学习率、batch size 等）。
在给出建议时，请简要说明“为什么”与“预期改进效果”。

【情况B：如果历史训练代码为空或为None】
请根据数据特征和上下文，推荐 **2–3 个适合的模型方案**。
要求：
1. 每个模型需包含模型名称、主要原理、适用场景；
2. 指出其在当前任务中的优势与潜在局限；
3. 保持语言专业、简洁，不输出代码。
