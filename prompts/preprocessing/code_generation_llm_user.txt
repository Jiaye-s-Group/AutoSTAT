=== 运行环境说明（已提供对象，禁止重复导入）===
运行环境中已提供以下对象与库，可直接使用：
- pandas DataFrame 变量：df
- numpy：np
- sklearn 相关对象：
  SimpleImputer、StandardScaler、MinMaxScaler、RobustScaler、
  OneHotEncoder、OrdinalEncoder、LabelEncoder、FunctionTransformer、
  ColumnTransformer、Pipeline
=== 数据概览 ===
- 数据规模：{{n_rows}} 行 × {{n_cols}} 列
- 数据类型分布：{{dtype_counts}}
- 缺失值总数：{{missing_total}}
- 各列缺失率：{{missing_by_col}}
- 数值型列：{{num_cols}}

=== 输入数据示例（前5行，仅用于推断列名与类型，不可假设示例覆盖全量数据）===
{{head_dict_str}}
=== LLM 返回的列级预处理建议（若为空则基于数据自行判断）===
{{refined_suggestions}}
