请严格只输出纯 Python 代码，不得包含以下任何内容：
- 解释性文字、注释、示例；
- Markdown 代码块标记（禁止出现 ``` 或 ```python 等）；
- 任何多余输出（如 print、display、日志输出等）；
- 任何 import / from import 语句。
请尽可能生成正确的代码！

=== 从算法库检索到的参考代码（仅供参考处理思路，禁止直接复制）===
{{knowledge_results}}
重要：上述参考代码可能包含 pd.read_csv、matplotlib、import 等当前环境不允许的操作。你只能参考其中的"数据处理思路与算法逻辑"（如 IQR 异常值判断方式、Z 得分计算公式、归一化计算方法等），必须严格按照下方白名单库重新实现，不得照搬参考代码。若参考代码为空或不相关，请忽略此段，根据数据和建议自行生成。

=== 第三方库使用白名单（严格限制）===
你只能使用以下第三方库/对象（即使环境中安装了更多库也禁止使用）：
- numpy：np
- sklearn 相关对象：
  SimpleImputer、StandardScaler、MinMaxScaler、RobustScaler、
  OneHotEncoder、OrdinalEncoder、LabelEncoder、FunctionTransformer、
  ColumnTransformer、Pipeline
- pandas：只允许通过已提供的 df 及其构造器使用（例如 type(df) 或 df.__class__ 创建 DataFrame），不得引用或假设存在 pd/pandas 变量，不得导入。
禁止使用未在上述列表中的任何第三方库与对象；禁止文件读写；禁止网络请求。

=== Python 内置函数说明 ===
运行环境支持所有 Python 内置函数（如 all、any、sorted、map、filter、type、getattr、round、zip、enumerate、isinstance、hasattr 等），可直接使用，无需导入。
但禁止使用：eval、exec、open、compile、input。

=== 强制输出约束（必须全部满足）===
1. 代码必须可直接执行且无语法错误；所有括号必须成对闭合。
2. 代码必须在执行后生成变量 process_df，并且脚本最后只保留一行：
   process_df = ...
   除此之外，最后不得再输出任何语句。
3. 禁止产生除 process_df 之外的"最终输出变量"；允许使用必要的中间局部变量与函数定义，但不得 print 或写文件。
4. 若发现 df 存在重复表头行（例如某行所有单元格与列名集合高度一致），必须自动删除该行（可删除多行）。
5. 必须保证 process_df 为 pandas.DataFrame；不得返回 numpy 数组、稀疏矩阵或 sklearn 的 Pipeline/Transformer 对象本身。
6. 确保 process_df 中每一列都有明确列名：
   - 若使用 OneHotEncoder / OrdinalEncoder 等产生新特征，必须生成可读的列名；
   - 若无法获取特征名，必须用可控规则生成（如 原列名__类别值 或 f0,f1,...），不得留空或重复。
7. 对类别特征：
   - 多列类别特征优先使用 OneHotEncoder 或 OrdinalEncoder；
   - 单列字符串/类别列必须使用 LabelEncoder 或 OrdinalEncoder，不得 passthrough。
8. 混合型列处理（同一列同时出现数值与字符串）必须在构建 ColumnTransformer 前完成：
   - 需检测混合型列（可对该列非空样本做数值可解析判断）；
   - 对混合型列使用 FunctionTransformer(lambda x: x.astype(str)) 统一为字符串类型后再编码。
9. 使用 OneHotEncoder 时：
   - 必须避免产生无法转 DataFrame 的稀疏输出；
   - 要么设置 OneHotEncoder(sparse_output=False)（若版本不支持则用 sparse=False），要么在变换后将稀疏矩阵转为 dense；
   - 最终必须构造 DataFrame。
10. 若用户需求与通用建议冲突，用户需求优先级最高；若某列被明确标注"无需处理"，不得对该列进行任何变换。
请严格依据以上要求，输出完整且可直接执行的纯 Python 代码（只输出代码，不输出任何其他内容）。