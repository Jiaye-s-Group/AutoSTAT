请根据以下预处理建议，保真精炼数据集中每一列的推荐预处理方法。精炼结果必须能逐项对应到“详细预处理建议”中的每一个具体处理方案，不能因为压缩而遗漏前端已展示的方案。

数据示例:
{{head_dict_str}}

详细预处理建议:
{{suggestion}}

=== 用户需求 ===
- 用户需求：{{user_input}}
- 用户的分析偏好设置：{{preference_selected}}
- 用户的建模目的与特殊需求（高优先级，务必满足）：{{add_preference}}
- 用户上传的参考资料（业务背景/算法说明/数据手册，如有相关内容请优先参考）：{{ref_context}}

输出要求（必须严格遵守）：
1. 输出格式：列名：方法1，方法2，方法3；每个列名独立换行。
2. 必须逐列覆盖数据示例和详细建议中出现的列名，不要合并列名。
3. 必须保留详细建议中每个具体方案的核心操作、对象、阈值、编码/填补/缩放方式和适用条件。
4. 只合并完全重复的同义操作；不要删除不同列、不同策略、不同阈值或不同处理目的的建议。
5. 输出必须为纯文本，不使用任何 Markdown 标记、编号、标题或解释。
