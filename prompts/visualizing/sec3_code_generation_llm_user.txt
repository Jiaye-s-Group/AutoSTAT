请根据以下上下文信息生成绘图代码：
【数据头部信息】
{{def_head}}

【颜色方案】
每一张图的颜色必须从以下列表中选择
{{color}}

【具体绘图建议】
{{refined_suggestions}}

【用户需求与偏好】
- 用户需求：{{user_input}}
- 用户的分析偏好设置：{{preference_selected}}
- 用户的建模目的与特殊需求（高优先级，务必满足）：{{add_preference}}
- 用户上传的参考资料（业务背景/算法说明/数据手册，如有相关内容请优先参考）：{{ref_context}}

结合上述数据特征和绘图建议，生成最终的 `fig_dict`。请尽可能生成正确代码，确保代码逻辑严密，处理好可能的缺失值或异常列名。

硬性输出协议：
- 代码必须显式创建 `fig_dict = {}`，并把每一张最终图保存为 `fig_dict["descriptive_unique_key"] = fig`。
- `fig_dict` 的 value 必须是 Plotly Figure 对象。
- 禁止只使用 `fig`、`figs`、`figures`、`charts`、`plots`、列表或函数内部变量保存图。
- 禁止调用 `.show()`、`print()`、`write_html()`、`write_image()` 或任何外部 IO。
- 如果生成了多张图，全部都必须进入同一个全局 `fig_dict`。
- 最终代码执行结束时，`fig_dict` 必须是非空字典。

重点：在代码编写时，严谨给df重新赋值，数据头部信息{{def_head}}的数据只是全局数据的前5行，编写代码时需要使用全局数据。

大数据绘图约束：
- 如果全量数据超过 5000 行，散点图、折线图、蜂群图、逐点时间序列等行级图表必须先采样、聚合或重采样，不要把全量行级数据直接放进 Plotly trace。
- 类别变量超过 20 个取值时，柱状图、饼图等必须只展示 top 20 或合并长尾类别。
- 时间序列优先按日、周、月等合适粒度聚合后绘制。
- 相关性、热力图、分布图、箱线图等可使用统计聚合结果，不要生成包含大量原始点的图对象。
- 采样时使用 `random_state=42`，例如 `plot_df = df.sample(n=min(len(df), 5000), random_state=42)`；不要覆盖 `df`。

编写代码满足具体绘图建议时，尽可能绘制精美的高水平科研图。

保证严格输出纯 Python 代码，不要输出任何解释性文字、注释、示例以及每段代码实现的功能解释。
补充硬性要求：
- 运行时已经直接提供全量 `df`，不要写任何 `df = ...`。
- `{{def_head}}` 只是全量数据的前 5 行预览，只能用于理解列名、类型和示例值，绝不能据此重新构造 `df`。
- 如果需要筛选、聚合、清洗，请使用新变量名，例如 `plot_df = df.copy()`、`agg_df = ...`。
- 一旦输出中出现 `df = pd.DataFrame(...)`、`df = pd.read_*...`、`df = df.head(...)`、`df = {...}`、`df = [...]`，都视为错误代码。
