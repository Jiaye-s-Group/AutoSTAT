任务：从以下代码中提取所有图表标题，并翻译成简体中文。

严格要求：
1. 只返回 JSON 数组。
2. 数组中每个元素都是一个中文标题字符串。
3. 按代码实际生成顺序输出。
4. 对循环、f-string、format、字符串拼接生成的标题要展开后再输出。
5. 不要漏掉任何一个标题。
6. 不要输出解释、说明、markdown、代码块。
7. 输出的标题中只含有中文，不含其他类型文字

代码：
{{final_code}}