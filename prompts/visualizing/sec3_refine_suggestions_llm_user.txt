请根据以下详细可视化建议，对图表方案进行保真精炼，输出可直接供下游绘图代码使用的配置列表。精炼结果必须逐项覆盖详细建议中的每一个具体图表方案，不能因为压缩、去重或追求图表多样性而遗漏前端已展示的方案。

详细可视化建议：
{{visual_recommendation}}

【用户需求与偏好】
- 用户需求：{{user_input}}
- 用户的分析偏好设置：{{preference_selected}}
- 用户的建模目的与特殊需求（高优先级，务必满足）：{{add_preference}}
- 用户上传的参考资料（业务背景/算法说明/数据手册，如有相关内容请优先参考）：{{ref_context}}

精简要求（必须严格遵守）：
1. 详细建议中的每个具体图表方案都必须保留对应配置。
2. 只有完全重复的图表方案才可以合并；变量、分组、图表类型、聚合方式、分析目的不同的方案不得合并或删除。
3. 必须保留每个方案的图表类型、列名/变量组合、分组字段、聚合指标、排序/Top N、时间粒度、分面或颜色分类等关键限定。
4. 不要为了减少重复图表类型而删除方案；如果详细建议要求多个柱状图、折线图或散点图，仍要保留。
5. 不要把详细建议中的图表类型替换成另一种图表类型，除非原文明确写成可选方案。
6. 不要新增详细建议中没有的图表、列名或分析角度。
7. 只输出最终配置，不输出理由、解释、过程或检查清单。
8. 输出为纯文本，每条独立一行。

输出格式（必须严格遵守）：
1.单变量格式：
列名: 图表1, 图表2
2.多变量格式：
关系组: 列A,列B: 图表1, 图表2
3.总体变量格式：
总体: 图表1, 图表2
4.严格不要添加标题、编号、示例、解释或任何额外文字。
5.每一行只保留精简后的最终图表方案。
6.同一行可列出同一列或同一关系组下的多个图表，但不得遗漏原文中的具体图表方案。
7. 如果详细建议已有 10 幅以上可执行图表，请完整保留；如果不足 10 幅，不要为凑数量编造新图。
